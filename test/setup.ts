import '@testing-library/jest-dom/vitest';

// Registers a per-test setup/cleanup so each render starts from a clean
// document (no leftover DOM between tests).
import '@testing-library/svelte/vitest';
